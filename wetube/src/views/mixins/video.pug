mixin video(video)
    div
        h4
            a(href=`/videos/${video.id}`)=video.title
        div
            span #{video.rating}/5
            span #{video.comments} comments.
            span Posted #{video.createdAt}
            span #{video.views} #{video.views === 1 ? "view" : "views"}.
